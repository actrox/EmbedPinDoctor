# P0 验收报告

## 验收范围

本次完成 EmbedPinDoctor 的 P0 命令行原型，目标是验证“芯片数据 + 模块数据 -> 自动接线表 -> 风险报告 -> Markdown 导出”的核心闭环。

## 已完成任务

| 编号 | 状态 | 说明 |
| --- | --- | --- |
| P0-01 | 已完成 | 已建立 `app`、`core`、`data`、`export`、`tests`、`output` 目录。 |
| P0-02 | 已完成 | 已定义芯片 JSON 数据结构。 |
| P0-03 | 已完成 | 已定义模块 JSON 数据结构。 |
| P0-04 | 已完成 | 已录入 `STM32F103C8T6` 基础引脚数据。 |
| P0-05 | 已完成 | 已录入 OLED、MPU6050、按键、蜂鸣器、SD 卡模块。 |
| P0-06 | 已完成 | 已实现芯片和模块 JSON 加载器。 |
| P0-07 | 已完成 | 已实现按接口功能筛选候选引脚。 |
| P0-08 | 已完成 | 已实现简单自动引脚分配。 |
| P0-09 | 已完成 | 已实现重复占用、调试脚、启动脚、未分配、电压不匹配、I2C 上拉提醒等检查。 |
| P0-10 | 已完成 | 已实现 Markdown 接线规划报告导出。 |

## 实际验证

已执行以下验证：

1. Python 语法编译：`app`、`core`、`export`、`tests` 下所有 `.py` 文件编译通过。
2. 冒烟测试：`tests/p0_smoke_test.py` 通过。
3. 风险测试：`tests/p0_risk_test.py` 通过。
4. CLI 导出：已生成 `output/demo_wiring.md`。

## 当前限制

- 当前还是 P0 命令行原型，不包含 Qt 图形界面。
- 芯片数据只完整录入了 STM32F103C8T6 的基础 P0 字段。
- 自动分配算法是简单评分策略，后续 P2 可继续优化。
- 当前只导出 Markdown，`pins.h` 属于 P1 阶段。

## 下一步建议

进入 P1：开始 Qt 桌面界面，实现新建项目、芯片选择、模块选择、引脚表格、风险报告和保存加载。
