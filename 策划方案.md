# 嵌入式引脚医生策划方案

## 1. 项目名称

暂定名称：**嵌入式引脚医生**

英文名称：**EmbedPinDoctor**

项目定位：一款面向嵌入式开发者的硬件接线、引脚分配、外设冲突检查和代码生成辅助软件。

## 2. 项目背景

嵌入式开发过程中，很多问题并不是代码本身造成的，而是出现在硬件接线、芯片引脚复用、启动脚误用、电平不匹配、外设资源冲突、调试口占用等环节。

现有工具通常只能解决某一部分问题：

- 原理图软件负责画电路，但不会主动理解开发意图。
- MCU 配置工具负责配置片上外设，但很少关心外接模块的真实使用场景。
- 笔记工具能记录接线，但不能自动检查冲突。
- 代码生成工具能生成初始化代码，但对硬件风险提示有限。

因此可以开发一款更贴近实际开发流程的软件：开发者只需要选择芯片和外设模块，软件自动推荐接线方案，检查潜在风险，并生成文档和代码模板。

## 3. 目标用户

### 3.1 主要用户

- 嵌入式初学者
- 单片机开发者
- 硬件爱好者
- 电子竞赛学生
- 物联网设备开发者
- 需要快速搭建原型的工程师

### 3.2 使用场景

- 新建一个 STM32 / ESP32 / RP2040 项目时，需要快速分配引脚。
- 使用多个模块时，需要检查 I2C、SPI、UART、PWM、ADC 等资源是否冲突。
- 画原理图前，需要先确认引脚规划是否合理。
- 写代码前，需要自动生成统一的引脚定义文件。
- 项目交接时，需要自动导出清晰的硬件接线文档。

## 4. 核心痛点

1. **引脚复用复杂**

   同一个引脚可能支持 GPIO、ADC、PWM、UART、SPI、I2C 等多种功能。开发者手动查表容易出错。

2. **启动脚和调试脚容易踩坑**

   例如 ESP32 的部分启动相关 GPIO、STM32 的 SWD 调试引脚，如果被错误占用，可能导致芯片无法启动或无法下载程序。

3. **外设资源冲突不直观**

   多个模块同时使用 SPI、UART、PWM、定时器、ADC 时，冲突往往到写代码或调试硬件时才暴露。

4. **接线文档容易过时**

   引脚变更后，代码、接线表、README、原理图标注常常不同步。

5. **模块电气特性容易忽略**

   比如 5V 与 3.3V 电平兼容性、I2C 上拉电阻、ADC 输入范围、电机驱动反灌电流等问题。

## 5. 产品目标

### 5.1 短期目标

做出一个可用的桌面软件原型，支持常见芯片和常见模块的引脚规划、冲突检查、文档导出和代码生成。

### 5.2 中期目标

形成可扩展的芯片数据库、模块数据库和规则检查系统，让用户可以自定义芯片、模块和检查规则。

### 5.3 长期目标

成为嵌入式项目早期设计阶段的硬件规划助手，连接原理图设计、固件开发和项目文档管理。

## 6. 核心功能设计

### 6.1 芯片选择

用户可以从芯片库中选择目标芯片。

第一阶段建议支持：

- STM32F103C8T6
- ESP32-WROOM-32
- RP2040

每个芯片需要记录：

- 引脚编号
- GPIO 名称
- 支持的外设功能
- ADC 通道
- PWM / 定时器能力
- UART / SPI / I2C 映射
- 启动相关引脚
- 调试相关引脚
- 默认电平状态
- 电压范围
- 特殊限制说明

### 6.2 模块选择

用户可以选择项目中需要使用的外部模块。

第一阶段建议支持：

- OLED I2C 屏幕
- MPU6050 姿态传感器
- DHT11 温湿度传感器
- 蜂鸣器
- 普通按键
- 旋转编码器
- TB6612 电机驱动
- WS2812 灯带
- SD 卡模块
- UART 串口模块

每个模块需要记录：

- 所需接口类型
- 所需引脚数量
- 是否需要中断引脚
- 是否需要 PWM
- 是否需要 ADC
- 工作电压
- 是否需要上拉电阻
- 是否对时序敏感
- 推荐接线方式
- 常见风险提示

### 6.3 自动引脚推荐

用户选择芯片和模块后，软件根据规则自动生成推荐接线表。

推荐逻辑包括：

- 优先使用专用外设引脚。
- 避免占用下载、调试、启动相关引脚。
- 避免把电机、蜂鸣器等上电敏感设备接到默认高电平引脚。
- 优先把共用总线设备放到同一条 I2C / SPI 总线上。
- 为中断模块选择支持外部中断的 GPIO。
- 为 PWM 设备选择定时器资源合理的引脚。
- 为 ADC 设备选择模拟输入引脚。

### 6.4 冲突检查

软件需要对当前引脚分配进行自动检查。

检查类型：

- 引脚重复占用
- 外设资源冲突
- 启动脚风险
- 调试脚风险
- 电压不匹配
- ADC / 数字功能冲突
- I2C 缺少上拉提醒
- SPI 片选脚不足
- PWM 定时器冲突
- UART 与下载串口冲突
- 上电默认电平风险

检查结果按严重程度分级：

- 错误：必须修改，否则项目可能无法运行。
- 警告：可以运行，但存在较高风险。
- 提示：建议优化，但不一定影响运行。

### 6.5 接线表生成

自动生成清晰的接线表。

示例：

| 模块 | 模块引脚 | 芯片引脚 | 功能 | 说明 |
| --- | --- | --- | --- | --- |
| OLED | SDA | PB7 | I2C1_SDA | I2C 数据线 |
| OLED | SCL | PB6 | I2C1_SCL | I2C 时钟线 |
| MPU6050 | INT | PA0 | GPIO_EXTI | 中断输入 |

### 6.6 代码生成

第一阶段可以生成简单的引脚定义文件。

例如生成 `pins.h`：

```c
#ifndef PINS_H
#define PINS_H

#define OLED_SCL_PIN GPIO_PIN_6
#define OLED_SCL_PORT GPIOB
#define OLED_SDA_PIN GPIO_PIN_7
#define OLED_SDA_PORT GPIOB

#define MPU6050_INT_PIN GPIO_PIN_0
#define MPU6050_INT_PORT GPIOA

#endif
```

后续可以扩展为：

- Arduino 引脚定义
- STM32 HAL 初始化代码
- ESP-IDF GPIO 配置代码
- PlatformIO 项目模板
- CMake 配置片段

### 6.7 文档导出

支持导出 Markdown 文档，内容包括：

- 项目名称
- 目标芯片
- 模块清单
- 接线表
- 风险检查报告
- 引脚定义代码
- 设计备注

### 6.8 用户自定义数据库

用户可以自定义：

- 新芯片
- 新模块
- 新检查规则
- 自定义引脚别名
- 自定义项目模板

第一阶段可以先使用 JSON 文件保存，后期再升级为 SQLite。

## 7. MVP 最小可用版本

MVP 不追求支持所有芯片，而是先验证软件核心价值。

### 7.1 MVP 必须包含

1. 芯片库：STM32F103C8T6、ESP32-WROOM-32、RP2040。
2. 模块库：OLED、MPU6050、DHT11、按键、蜂鸣器、电机驱动、SD 卡。
3. 图形界面：芯片选择、模块选择、接线表、风险报告。
4. 自动推荐：根据模块需求自动分配引脚。
5. 冲突检查：至少检查重复占用、启动脚、调试脚、电压、I2C、SPI。
6. 导出功能：导出 Markdown 和 `pins.h`。

### 7.2 MVP 可以暂缓

- 原理图绘制
- PCB 布线
- 在线芯片数据库
- 自动读取 KiCad 工程
- 复杂 AI 推荐
- 大规模芯片支持

## 8. 软件界面规划

### 8.1 主界面结构

建议使用三栏布局：

1. 左侧：项目和芯片选择。
2. 中间：模块列表和引脚分配表。
3. 右侧：风险检查报告。
4. 底部：导出、保存、生成代码按钮。

### 8.2 页面模块

- 项目首页
- 新建项目向导
- 芯片选择页
- 模块选择页
- 引脚分配页
- 风险报告页
- 导出设置页
- 数据库管理页

### 8.3 交互方式

- 用户可以自动分配，也可以手动修改。
- 手动修改后，软件实时重新检查风险。
- 风险项点击后，高亮相关引脚和模块。
- 导出前显示最终检查结果。

## 9. 技术选型建议

### 9.1 推荐方案：Qt + C++

适合做严肃桌面软件，性能稳定，界面成熟，跨平台能力强。

推荐结构：

```text
EmbedPinDoctor/
├── app/                  # 应用入口和窗口管理
├── core/                 # 核心引脚分配和规则检查逻辑
├── data/                 # 芯片库、模块库、规则库
├── export/               # 文档和代码导出
├── ui/                   # Qt 界面
├── tests/                # 单元测试
└── docs/                 # 项目文档
```

### 9.2 数据格式

第一阶段建议使用 JSON：

```json
{
  "chip": "STM32F103C8T6",
  "pins": [
    {
      "name": "PB6",
      "functions": ["GPIO", "I2C1_SCL", "TIM4_CH1"],
      "risks": [],
      "voltage": "3.3V"
    }
  ]
}
```

后期如果数据变多，可以迁移到 SQLite。

### 9.3 核心算法

核心可以分为三层：

1. **需求建模**

   把模块需求转换为结构化约束，例如：需要 I2C、需要 2 个 GPIO、需要 1 个 PWM。

2. **候选引脚搜索**

   根据芯片能力筛选可用引脚，生成候选集合。

3. **评分和分配**

   给每个候选方案打分，避开高风险引脚，优先选择更稳定的方案。

简单评分示例：

- 使用启动脚：-50 分
- 使用调试脚：-40 分
- 使用推荐外设引脚：+30 分
- 多个 I2C 模块共用同一总线：+20 分
- 电压不匹配：直接判定错误

## 10. 规则系统设计

规则可以设计成可扩展结构。

规则示例：

```json
{
  "id": "avoid_debug_pins",
  "level": "warning",
  "message": "该引脚属于调试接口，可能影响下载和调试。",
  "condition": {
    "pin_tag": "debug"
  }
}
```

第一阶段可以先用 C++ 写死核心规则，等功能稳定后再开放 JSON 规则配置。

## 11. 风险报告示例

```text
项目：四轴姿态采集板
芯片：STM32F103C8T6

发现 5 个问题：

[错误] PA13 被分配给 OLED_SCL，但 PA13 是 SWDIO 调试引脚，可能导致程序无法下载。
[警告] PB2 是启动相关引脚，不建议连接带默认下拉的外设。
[警告] OLED 和 MPU6050 共用 I2C，总线可以复用，但需要确认上拉电阻。
[提示] 蜂鸣器连接到上电默认高电平引脚，可能开机短暂鸣叫。
[提示] SD 卡模块请确认是否支持 3.3V 逻辑电平。
```

## 12. 开发阶段规划

### 第一阶段：策划和原型

目标：确定产品形态和数据结构。

任务：

- 完成需求文档。
- 设计芯片 JSON 数据格式。
- 设计模块 JSON 数据格式。
- 做一个简单命令行原型，验证自动分配逻辑。
- 准备 3 个芯片和 5 到 10 个模块的数据。

### 第二阶段：核心逻辑

目标：完成引脚分配和冲突检查。

任务：

- 实现芯片数据库加载。
- 实现模块数据库加载。
- 实现引脚候选搜索。
- 实现简单评分系统。
- 实现风险检查器。
- 实现 Markdown 和 `pins.h` 导出。

### 第三阶段：桌面界面

目标：做出可操作的软件界面。

任务：

- 新建项目界面。
- 芯片选择界面。
- 模块选择界面。
- 引脚分配表格。
- 风险报告面板。
- 导出按钮。

### 第四阶段：完善和扩展

目标：提升实用性。

任务：

- 增加用户自定义芯片。
- 增加用户自定义模块。
- 增加项目保存和加载。
- 增加更多芯片型号。
- 增加更多代码生成模板。
- 增加 KiCad 标签导出。

## 13. 项目亮点

1. **从模块需求出发，而不是从芯片引脚出发。**

   用户不需要先研究芯片手册，只需要告诉软件自己要用什么模块。

2. **提前发现硬件风险。**

   在画板和焊接之前发现问题，减少返工。

3. **接线、代码、文档同步生成。**

   避免文档和代码不一致。

4. **适合教学和工程实践。**

   初学者可以学习正确接线方式，工程师可以节省项目初始化时间。

## 14. 竞争差异

与 STM32CubeMX 相比：

- STM32CubeMX 偏向芯片外设配置。
- 本软件偏向外部模块接线和硬件风险检查。

与 KiCad 相比：

- KiCad 偏向原理图和 PCB 设计。
- 本软件偏向前期引脚规划和开发文档生成。

与普通笔记工具相比：

- 普通笔记工具只能记录。
- 本软件可以自动检查和生成。

## 15. 后续可扩展方向

- KiCad 原理图标签导出
- PlatformIO 项目生成
- Arduino 示例代码生成
- STM32CubeMX `.ioc` 辅助生成
- ESP-IDF `sdkconfig` 辅助生成
- 硬件版本管理
- 团队协作项目库
- 常见模块接线知识库
- AI 辅助解释风险原因
- 根据已有代码反推引脚使用情况

## 16. 推荐第一步

建议先不要直接做完整桌面软件，而是先做一个小型核心原型：

1. 准备 `chips/stm32f103c8t6.json`。
2. 准备 `modules/oled_i2c.json`、`modules/mpu6050.json`、`modules/button.json`。
3. 写一个核心分配程序。
4. 输入芯片和模块列表。
5. 输出接线表和风险报告。

只要这个命令行原型能跑通，再开始做 Qt 图形界面会更稳。

## 17. 功能优先级拆分

为了方便后续开发，所有功能按 `P0 / P1 / P2 / P3` 分级。

优先级含义：

- **P0：必须先做。** 没有这些功能，软件无法验证核心价值。
- **P1：MVP 必须具备。** 有了 P0 和 P1，软件可以形成第一个可演示版本。
- **P2：增强体验。** 不影响核心闭环，但会明显提升实用性。
- **P3：长期扩展。** 适合核心稳定后再做，避免早期开发范围失控。

### 17.1 P0：核心闭环

P0 目标：先证明软件能根据芯片和模块生成接线建议，并发现基础风险。

| 编号 | 功能 | 目标 | 输出 | 验收标准 |
| --- | --- | --- | --- | --- |
| P0-01 | 项目基础结构 | 建立核心代码、数据、导出、文档目录 | 项目目录和基础 README | 能清楚区分 `core`、`data`、`export`、`docs` |
| P0-02 | 芯片数据格式 | 定义芯片 JSON 结构 | `chips/*.json` 示例 | 能描述引脚、功能、风险标签、电压 |
| P0-03 | 模块数据格式 | 定义模块 JSON 结构 | `modules/*.json` 示例 | 能描述接口需求、引脚需求、电压、风险 |
| P0-04 | STM32F103C8T6 基础数据 | 建立第一个芯片样例 | `stm32f103c8t6.json` | 至少覆盖 GPIO、I2C、SPI、UART、ADC、SWD 风险 |
| P0-05 | 常见模块基础数据 | 建立第一批模块样例 | OLED、MPU6050、按键、蜂鸣器、SD 卡模块 | 每个模块能表达接口需求 |
| P0-06 | 数据加载器 | 读取芯片和模块 JSON | 加载接口 | 程序能读取并校验基础字段 |
| P0-07 | 引脚候选筛选 | 根据模块需求筛选可用引脚 | 候选引脚列表 | I2C、SPI、GPIO、PWM、ADC 能分别筛选 |
| P0-08 | 简单自动分配 | 自动生成第一版接线表 | 接线表数据 | 输入芯片和模块后能输出不重复的分配结果 |
| P0-09 | 基础冲突检查 | 检查重复占用、调试脚、启动脚 | 风险列表 | 故意制造冲突时能输出错误或警告 |
| P0-10 | Markdown 导出 | 导出项目接线文档 | `README.md` 或 `wiring.md` | 文档包含芯片、模块、接线表、风险报告 |

P0 完成标志：命令行原型可以输入一个芯片和若干模块，输出接线表与风险报告。

### 17.2 P1：MVP 图形版本

P1 目标：把核心能力包装成可用的桌面软件，让用户可以通过界面完成一次完整项目规划。

| 编号 | 功能 | 目标 | 输出 | 验收标准 |
| --- | --- | --- | --- | --- |
| P1-01 | Qt 桌面项目初始化 | 建立可运行桌面程序 | Qt 工程 | 能启动主窗口 |
| P1-02 | 新建项目界面 | 输入项目名称并选择芯片 | 新建项目页面 | 能创建项目数据对象 |
| P1-03 | 芯片选择界面 | 展示芯片库 | 芯片列表 | 能选择 STM32F103C8T6 / ESP32 / RP2040 |
| P1-04 | 模块选择界面 | 展示模块库并添加模块 | 模块选择页面 | 能添加多个模块到项目 |
| P1-05 | 引脚分配表格 | 展示自动分配结果 | 表格组件 | 能看到模块引脚、芯片引脚、功能、说明 |
| P1-06 | 风险报告面板 | 展示错误、警告、提示 | 风险列表 | 风险按级别区分显示 |
| P1-07 | 手动修改引脚 | 用户可调整自动分配结果 | 可编辑表格 | 修改后重新检查冲突 |
| P1-08 | `pins.h` 导出 | 生成 C/C++ 引脚定义 | `pins.h` | 文件内容能用于后续固件项目 |
| P1-09 | 项目保存和加载 | 保存当前芯片、模块、引脚方案 | 项目 JSON | 关闭后重新打开能恢复数据 |
| P1-10 | 基础异常提示 | 数据缺失或分配失败时提示原因 | 错误提示 | 不出现无说明的失败 |

P1 完成标志：用户可以通过图形界面完成“选择芯片 -> 添加模块 -> 自动分配 -> 查看风险 -> 导出文档和代码”。

### 17.3 P2：实用增强

P2 目标：提升工程实用性，让软件能适应更多真实项目。

| 编号 | 功能 | 目标 | 输出 | 验收标准 |
| --- | --- | --- | --- | --- |
| P2-01 | 更多芯片支持 | 扩展芯片数据库 | ESP32-WROOM-32、RP2040 完整数据 | 每个芯片都有风险标签和常用外设映射 |
| P2-02 | 更多模块支持 | 扩展模块数据库 | 电机驱动、编码器、LoRa、继电器、ADC 传感器等 | 模块覆盖常见接口类型 |
| P2-03 | 评分系统优化 | 给候选方案打分 | 分配评分结果 | 推荐方案能避开高风险引脚 |
| P2-04 | 风险解释 | 解释每条风险原因 | 风险详情面板 | 用户能看到原因和建议修改方式 |
| P2-05 | 用户自定义模块 | 允许新增模块 | 模块编辑器 | 新模块保存后可参与分配 |
| P2-06 | 用户自定义芯片 | 允许新增芯片 | 芯片编辑器 | 新芯片保存后可被选择 |
| P2-07 | 导出模板系统 | 支持不同代码模板 | Arduino / STM32 HAL / ESP-IDF 模板 | 用户可选择导出目标平台 |
| P2-08 | KiCad 标签导出 | 输出原理图可用标签 | KiCad label 文件或文本 | 标签名称和引脚分配一致 |
| P2-09 | 搜索和筛选 | 快速查找芯片、模块、风险 | 搜索框和筛选器 | 大量数据下仍能快速定位 |
| P2-10 | 项目备注 | 记录设计说明 | 备注字段 | 备注能随项目保存和导出 |

P2 完成标志：软件不只是演示工具，而是可以用于真实小型嵌入式项目规划。

### 17.4 P3：长期扩展

P3 目标：在核心稳定后扩展生态能力和智能能力。

| 编号 | 功能 | 目标 | 输出 | 验收标准 |
| --- | --- | --- | --- | --- |
| P3-01 | 在线芯片数据库 | 从云端更新芯片数据 | 数据更新功能 | 用户可下载新芯片包 |
| P3-02 | 在线模块数据库 | 从云端更新模块数据 | 模块更新功能 | 用户可下载新模块包 |
| P3-03 | AI 风险解释 | 用 AI 解释复杂硬件风险 | 智能解释面板 | 能把风险转成易懂建议 |
| P3-04 | 从代码反推引脚 | 分析已有固件代码 | 引脚使用报告 | 能从 `#define` 或配置文件提取引脚 |
| P3-05 | STM32CubeMX `.ioc` 辅助生成 | 生成或辅助编辑 `.ioc` | `.ioc` 片段或模板 | 能减少 CubeMX 手动配置工作 |
| P3-06 | PlatformIO 项目生成 | 生成完整工程模板 | PlatformIO 工程 | 能直接打开并编译基础项目 |
| P3-07 | 团队协作 | 多人维护同一项目数据 | 协作项目格式 | 支持版本管理和变更记录 |
| P3-08 | 硬件版本管理 | 管理不同板卡版本 | 版本对比报告 | 能比较 V1 / V2 引脚变化 |
| P3-09 | 原理图导入 | 从 KiCad 原理图读取网络标签 | 导入器 | 能和已有硬件设计互相校验 |
| P3-10 | 插件系统 | 允许第三方扩展芯片、模块、导出器 | 插件接口 | 外部插件能注册新能力 |

P3 完成标志：软件从单机工具逐步变成嵌入式硬件规划平台。

## 18. 推荐开发顺序

建议不要先做完整界面，而是先做核心命令行原型。原因是本项目真正的技术价值在于数据结构、规则检查和分配算法，界面应建立在稳定核心之上。

推荐顺序：

1. **P0-02 芯片数据格式**
2. **P0-03 模块数据格式**
3. **P0-04 STM32F103C8T6 基础数据**
4. **P0-05 常见模块基础数据**
5. **P0-06 数据加载器**
6. **P0-07 引脚候选筛选**
7. **P0-08 简单自动分配**
8. **P0-09 基础冲突检查**
9. **P0-10 Markdown 导出**
10. **P1-01 Qt 桌面项目初始化**
11. **P1-02 到 P1-10 图形界面和导出完善**

## 19. P0 详细任务卡

下面这些任务可以直接作为后续开发任务使用。

### P0-01 项目基础结构

- **目标**：建立软件项目的基础目录结构。
- **负责人**：架构 / Qt 开发。
- **输入**：本策划方案。
- **输出**：项目根目录、README、基础目录。
- **验收**：目录能清楚区分核心逻辑、数据、导出、界面和测试。
- **风险**：如果一开始目录混乱，后续数据和界面容易耦合。

### P0-02 芯片数据格式

- **目标**：定义芯片 JSON 数据结构。
- **负责人**：核心逻辑开发。
- **输入**：STM32F103C8T6 数据手册、ESP32 引脚资料、RP2040 引脚资料。
- **输出**：芯片 JSON schema 和示例文件。
- **验收**：能表达引脚名称、可用功能、风险标签、电压、备注。
- **风险**：不同芯片的复用规则差异大，早期格式要保留扩展字段。

### P0-03 模块数据格式

- **目标**：定义外部模块 JSON 数据结构。
- **负责人**：核心逻辑开发。
- **输入**：常见模块资料。
- **输出**：模块 JSON schema 和示例文件。
- **验收**：能表达接口需求、引脚数量、工作电压、特殊限制。
- **风险**：模块需求不能只写文字说明，必须结构化，否则无法自动分配。

### P0-04 STM32F103C8T6 基础数据

- **目标**：录入第一个可用芯片数据。
- **负责人**：嵌入式资料整理 / 核心逻辑开发。
- **输入**：STM32F103C8T6 引脚资料。
- **输出**：`data/chips/stm32f103c8t6.json`。
- **验收**：至少支持 I2C1、SPI1、USART1、ADC、GPIO、SWD 风险标签。
- **风险**：必须标注 PA13 / PA14 调试引脚，避免推荐占用。

### P0-05 常见模块基础数据

- **目标**：录入第一批模块数据。
- **负责人**：嵌入式资料整理 / 核心逻辑开发。
- **输入**：OLED、MPU6050、按键、蜂鸣器、SD 卡模块资料。
- **输出**：`data/modules/*.json`。
- **验收**：每个模块都能被加载并转成约束需求。
- **风险**：SD 卡模块电平和 SPI 需求需要明确标注。

### P0-06 数据加载器

- **目标**：实现读取芯片和模块数据的代码。
- **负责人**：核心逻辑开发。
- **输入**：芯片 JSON、模块 JSON。
- **输出**：数据加载类和基础字段校验。
- **验收**：数据缺字段时能报告具体错误。
- **风险**：不要让界面层直接读 JSON，避免后续耦合。

### P0-07 引脚候选筛选

- **目标**：根据模块需求找出可用引脚。
- **负责人**：核心逻辑开发。
- **输入**：芯片数据、模块需求。
- **输出**：候选引脚列表。
- **验收**：I2C、SPI、UART、GPIO、PWM、ADC 都有对应筛选逻辑。
- **风险**：候选筛选和最终评分要分开，便于后续优化。

### P0-08 简单自动分配

- **目标**：生成第一版自动接线方案。
- **负责人**：核心逻辑开发。
- **输入**：候选引脚列表、模块需求。
- **输出**：接线表数据结构。
- **验收**：多个模块不会重复占用同一引脚，I2C 设备可以共用总线。
- **风险**：早期不需要追求最优解，但必须保证结果可解释。

### P0-09 基础冲突检查

- **目标**：对接线方案进行风险检查。
- **负责人**：核心逻辑开发。
- **输入**：接线表、芯片风险标签、模块风险标签。
- **输出**：错误 / 警告 / 提示列表。
- **验收**：重复占用、调试脚、启动脚、电压不匹配能被识别。
- **风险**：风险级别要稳定，避免用户不知道哪些必须修改。

### P0-10 Markdown 导出

- **目标**：导出可读的项目文档。
- **负责人**：导出模块开发。
- **输入**：项目数据、接线表、风险报告。
- **输出**：Markdown 文档。
- **验收**：导出文件包含芯片、模块、接线表、风险报告。
- **风险**：导出内容要来自同一份项目数据，避免和界面显示不一致。

## 20. 第一版验收标准

第一版命令行原型验收标准：

1. 可以加载 `STM32F103C8T6` 芯片数据。
2. 可以加载至少 5 个模块数据。
3. 可以自动生成接线表。
4. 可以发现重复占用、调试脚、启动脚、电压不匹配。
5. 可以导出 Markdown 风险报告。
6. 出错时能说明具体原因，而不是直接崩溃。

第一版图形 MVP 验收标准：

1. 可以新建项目。
2. 可以选择芯片。
3. 可以添加模块。
4. 可以查看和编辑接线表。
5. 可以查看风险报告。
6. 可以导出 Markdown 和 `pins.h`。
7. 可以保存和重新打开项目。

