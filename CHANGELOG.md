# Changelog

## Unreleased

- 完成 P4 数据与插件生态：统一生态包清单、语义版本、最低应用版本、Schema 与 SHA-256 校验。
- 新增事务式安装、覆盖备份、安全卸载、降级保护和启停状态，并在 Web 工作台提供管理入口。
- 插件注册表增加 capability 白名单、错误隔离及内置/用户插件合并发现。

- 完成 P3 发布与日常可用性：项目 schema v2 自动迁移、原子保存、最多 30 个历史版本及撤销/重做。
- 启动器改为动态端口与健康检查，新增轮转日志、未捕获异常记录和单文件运行数据目录。
- 诊断包默认排除项目数据，新增 Windows 单文件构建配置、发布校验器和 Python 3.11/3.12 CI。

- 完成 P2 推荐闭环：引脚锁定、最小变更策略、选择理由、多方案枚举和方案差异。
- 新增 I2C 地址、SPI 模式与频率、UART 硬件流控和 PWM 定时器频率检查。
- Web 工作台增加分配策略、锁定控件和可采用的备选方案卡。
- 完成 P1 只读项目扫描：PlatformIO、Arduino、ESP-IDF、STM32CubeMX 和 KiCad 工程识别。
- 引脚引用记录文件、行号、原始片段和置信度，并与医生分配方案进行三方差异诊断。
- Web 工作台支持填写现有工程目录、查看扫描摘要、来源列表和可复制修复片段。
- 完成可信诊断 P0：版本化数据 Schema、整包导入校验和数据审计命令。
- 使用有界回溯替换贪心引脚分配，保证确定性并优先寻找完整可行方案。
- 新增输入专用脚、输入过压、GPIO 过流、5V 容忍和开漏能力检查。
- 风险评分加入置信度权重，未人工签字的芯片数据会明确提示。

## 0.1.0-alpha.6

- 完成 P0-P6：核心引脚规划、Web MVP、工程增强、平台扩展、产品化准备和发布基础。
- 新增自动打开浏览器的启动器。
- 新增日志、示例项目、诊断包和发布目录包。
